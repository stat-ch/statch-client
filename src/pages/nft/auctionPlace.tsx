import MenuButton from "components/MenuButton";
import React from "react";

const AuctionPlace = () => {
  return (
    <div>
      <MenuButton label="test" active={false} />
    </div>
  );
};

export default AuctionPlace;
